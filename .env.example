# Copy to .env.local (gitignored) and fill in. Also set these in Vercel → Settings → Environment Variables.

# MongoDB Atlas — session counter + live presence
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>.mongodb.net/?appName=Cluster0

# Azure Blob Storage — generated meditation MP3s (optional)
AZURE_STORAGE_CONNECTION_STRING=
